{
  "name": "time_capsule",
  "scripts": {
    "install": "cd front && npm i --production=false && cd ../server && npm i --production=false",
    "start": "npm run front & npm run server",
    "front": "cd front && npx yarn start",
    "server": "cd server && PORT=5000 npx nodemon server.js"
  }
}
